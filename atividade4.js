for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "1 *" + tabuada + "=" + 1 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "2 *" + tabuada + "=" + 2 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "3 *" + tabuada + "=" + 3 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "4 *" + tabuada + "=" + 4 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "5 *" + tabuada + "=" + 5 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "6 *" + tabuada + "=" + 6 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "7 *" + tabuada + "=" + 7 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "8 *" + tabuada + "=" + 8 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "9 *" + tabuada + "=" + 9 * tabuada;
    console.log(row)
}

for(var tabuada = 1; tabuada <= 10; tabuada ++ ) {
    var row = "10 *" + tabuada + "=" + 10 * tabuada;
    console.log(row)
}